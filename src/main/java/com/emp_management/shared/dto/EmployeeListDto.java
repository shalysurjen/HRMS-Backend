package com.emp_management.shared.dto;

public interface EmployeeListDto {
    String getEmpId();    // → alias must be "empId"
    String getEmpName();  // → alias must be "empName"
    String getRole();     // → alias must be "role"
}
